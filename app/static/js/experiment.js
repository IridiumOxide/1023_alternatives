'use strict'

var startExperiment = function(feedback, time_limited, time_amount, nickname, training_length){
    setrandom(); // temporary show
    return;
    
    /*
    var already_used = [];
    for(int i = 0; i < 1024; ++i){
        already_used = false;
    }
    prepare_user(nickname);
    
    var stopped = false;
    while(!stopped){
        
    }    
    */
}
